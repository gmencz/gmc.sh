- name: allowed-queries
  definition:
    queries:
      - name: Me
        query:
          "query Me($id: String!) {\r\n  users_by_pk(id: $id)
          {\r\n    id\r\n    name\r\n
          \   company\r\n    picture\r\n    verified\r\n    last_seen\r\n  }\r\n}"
